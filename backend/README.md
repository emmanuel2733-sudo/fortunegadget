# Backend (Paystack)

## Setup

1. Copy `.env.example` to `.env`.
2. Put your Paystack secret key in `PAYSTACK_SECRET_KEY`.
3. Install dependencies:
   - `npm install`
4. Start server:
   - `npm run dev`

Server runs on `http://localhost:4242` by default.
